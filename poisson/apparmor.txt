#include <tunables/global>

profile poisson flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Allow all access — default HA add-on profile
  file,
  signal,
  network,
  capability,
  mount,
  umount,
}
